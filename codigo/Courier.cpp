#include "Courier.h"

Courier::Courier(const std::string &licensePlate, double volMax, double pesoMax, double transportFee) : licensePlate(
        licensePlate), volMax(volMax), pesoMax(pesoMax), transportFee(transportFee) {}
